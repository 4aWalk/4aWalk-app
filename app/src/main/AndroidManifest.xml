<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme._4aWalk"
        tools:targetApi="31">
        <activity
            android:name=".activites.ActivitePrincipale"
            android:theme="@style/Theme.AppCompat"
            android:networkSecurityConfig="@xml/network_security_config"
            android:exported="true"
            tools:targetApi="31" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activites.ActiviteInscription"
            android:exported="false" />

        <activity android:name=".activites.ActiviteListes"/>

        <activity android:name=".activites.ActiviteGestionRandonnee"/>

        <activity
            android:name=".GestionP.Activity.PopUpParticipantActivity"
            android:exported="true"/>

        <activity
            android:name=".GestionP.Activity.SacActivity"
            android:exported="true"/>
    </application>

</manifest>